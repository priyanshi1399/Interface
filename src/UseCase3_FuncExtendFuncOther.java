/*@FunctionalInterface
public interface UseCase3_FuncExtendFuncOther extends UseCase3_FuncExtendFunc{

    void canFly(String val);
    //This si not allowed because this one also has parent one method so having 2 not allowed but to overcome this
    //we need to have same return type then it is allowed see below code this is allowed
    //or check in UseCase3Sol_FuncExtendFunc Interface
}
*/